# CLIProxyAPI 模块更新配置（自动生成：__REPO__）
# 手动编辑此文件即可，保存后无需重启模块。

# 自动更新总开关: 1 = 开机时自动检测并安装新模块版; 0 = 仅手动更新 (默认)
CF_AUTO=0

# 自动构建(重新打包)模块的 GitHub 仓库 <owner>/<repo>
UPDATE_REPO=__REPO__

# 上游源项目（仅用于版本检测）
SOURCE_REPO=router-for-me/CLIProxyAPI